spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
  h2:
    console:
      enabled: true

jwt:
  secret: TestSecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLong
  expiration: 86400000

voting:
  security:
    max-votes-per-ip: 10
    ip-restriction-hours: 24
